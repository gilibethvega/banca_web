<div class="card-bancaweb">
  <h2 class="my-4 mx-auto text-center">Inicia sesión 🦸🏻‍♀️✨🦸🏻‍♂️</h2>
    <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
      <div class="field">
        <%= f.label :email, class:"form-label" %><br />
        <%= f.email_field :email, autofocus: true, autocomplete: "email", class:"form-control" %>
      </div>

      <div class="field">
        <%= f.label :password, class:"form-label" %><br />
        <%= f.password_field :password, autocomplete: "current-password", class:"form-control" %>
      </div>

      <% if devise_mapping.rememberable? %>
        <div class="field d-flex align-middle">
          <%= f.check_box :remember_me %>
          <%= f.label :remember_me, "Recuerdame", class:"mx-2" %>
        </div>
      <% end %>

      <div class="actions text-center">
        <%= f.submit "Ingresar", class:"btn btn-primary px-4 py-2" %>
      </div>
    <% end %>

    <%= render "devise/shared/links" %>
</div>