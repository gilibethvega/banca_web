<%= form_with(model: friendly_type, local: true) do |form| %>
  <% if friendly_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(friendly_type.errors.count, "error") %> prohibited this friendly_type from being saved:</h2>

      <ul>
      <% friendly_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.text_field :image %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
